{
  "name": "comp214-group-project",
  "version": "1.0.0",
  "workspaces": [
    "packages/frontend"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/frontend dev",
    "start": "bun run --cwd packages/frontend start",
    "build": "bun run --cwd packages/frontend build"
  },
  "devDependencies": {
    "frontend": "workspace:*"
  }
}
